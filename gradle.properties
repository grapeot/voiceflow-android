# JVM memory for the Gradle daemon (avoid GC thrashing during Android builds).
org.gradle.jvmargs=-Xmx2048m
# Use AndroidX artifacts (required by AGP).
android.useAndroidX=true
# Official Kotlin code style.
kotlin.code.style=official
